kind: Service
apiVersion: v1
metadata:
  name: backend
  labels:
    app: backend
spec:
  ports:
    - port: 8080
      targetPort: 4000
      nodePort: 31200
  selector:
    app: backend
  type: LoadBalancer